# T1: implement invoice status filtering

Synthetic example only. Before real delegation, inspect and supply actual repo
paths and the exact existing Invoice type. Do not run this as a real task here.

Deliverable: filter_invoices(invoices, status=None) preserves input order and
record identity; returns a new list; accepts open, paid, void or None; raises
ValueError for other statuses. Preserve the input list. No provider or payment
logic, dependencies or production data. Allowed paths: src/invoices/filtering.py
and tests/test_invoice_filtering.py. Astra supplies the actual workspace baseline.

Test the no-filter, each status, empty input, invalid status, order and non-mutation
cases. Run python3 -m unittest discover -s tests -p 'test_invoice_filtering.py'.
Implement and test the entire behavior in one worker assignment rather than
spawning a worker for each test. Return changed paths, actual exits and remaining
risks. Astra independently reviews behavior and implementation before accepting.
