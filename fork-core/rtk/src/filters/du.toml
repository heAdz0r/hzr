[filters.du]
description = "Compact du output"
match_command = "^du\\b"
strip_lines_matching = ["^\\s*$"]
truncate_lines_at = 120
max_lines = 40
# Every row of `du -s` (and of depth-limited output) is an independent answer for
# its own root; a line cap hides the largest consumers while still reporting a
# confident total. Run those shapes raw instead of truncating the answer.
pass_through_if_args = ["-s", "--summarize", "-d", "--max-depth"]

[[tests.du]]
name = "preserves sizes, strips blank lines"
input = "4.0K\t./src\n\n8.0K\t./tests\n16K\t."
expected = "4.0K\t./src\n8.0K\t./tests\n16K\t."

[[tests.du]]
name = "single line passthrough"
input = "128K\t."
expected = "128K\t."
